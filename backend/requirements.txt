# KenzySites - Minimal Backend Dependencies
# Python 3.12+ required

# Core Framework - minimal for initial deploy
fastapi==0.100.1
uvicorn==0.23.2
pydantic==1.10.12

# Basic HTTP
requests==2.31.0
python-dotenv==1.0.0

# Redis only (PostgreSQL via environment)  
redis==4.6.0